{
  "name": "timetable-scheduler",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && rm -rf frontend_dist && cp -r frontend/dist frontend_dist"
  }
}
